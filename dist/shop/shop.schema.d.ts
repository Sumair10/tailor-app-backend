import * as mongoose from 'mongoose';
export declare const ShopSchema: mongoose.Schema<any, mongoose.Model<any, any, any, any, any>, {}, {}, any, {}, "type", {
    name: string;
}>;
export interface Shop {
    name: string;
}
