import * as mongoose from 'mongoose';
export declare const OrgSchema: mongoose.Schema<any, mongoose.Model<any, any, any, any, any>, {}, {}, any, {}, "type", {
    name: string;
}>;
export interface Org {
    name: string;
}
